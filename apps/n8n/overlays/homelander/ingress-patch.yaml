apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: n8n
  namespace: n8n
spec:
  tls:
    - hosts:
        - n8n.homelander.local
      secretName: n8n-tls
  rules:
    - host: n8n.homelander.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: n8n
                port:
                  number: 80
